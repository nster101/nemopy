[build-system]
requires = ["maturin>=1.5"]
build-backend = "maturin"

[project]
name = "nemopy-rust-core"
version = "0.1.0"
description = "Rust acceleration extension for nemopy (optional)."
requires-python = ">=3.9"

[tool.maturin]
module-name = "nemopy._rust_core"
